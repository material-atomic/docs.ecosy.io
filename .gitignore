node_modules
.env*
.next
next-env.d.ts
/dist/
.vinext/
.wrangler/

# guard: downloaded npm tarballs + extracted packages, and per-run reports.
# Keyed by name@version (lib/registry.mjs) — re-fetched only when a
# package's published `latest` changes, reused across runs otherwise. Not
# source, not committed.
/guard/.cache/
/guard/last-run.json
